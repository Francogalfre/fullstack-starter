import type { NextPage } from 'next'
import { AppShell, Navbar, Header, Button } from '@mantine/core';

const Home: NextPage = () => {
  return (
    <Button>
      Hi
    </Button>
  )
}

export default Home
